<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>T-Gene Input Template</title>
    <link href="../doc/css/style.css" rel="stylesheet" type="text/css">
    <script src="../doc/js/utilities.js"></script>
    <script src="../js/site.js"></script>
    <script src="../js/component_tgene.js"></script>
    <script>
      function XXXXregister_componentXXXX(uid, component) {

      }
    </script>
  </head>
  <body>
    <div class="pop_content" id="pop_${prefix}">
      <p>Information about the panel field would be displayed here.</p>
      <div class="pop_close">[ <a href="javascript:help_popup()">close</a> ]</div>
    </div>
    <!--{help}-->
    <!--{/help}-->
    <!--{component}-->
          <h2 class="meme"><!--{title}-->Select the histone + expression panel<!--{/title}--></h2>
          <div class="subcontent">
            <h4 class="meme"><!--{subtitle}-->Choose a panel corresponding to the genome of your loci<!--{/subtitle}-->
            <div class="help" data-topic="pop_${prefix}"></div></h4>
            <div class="tgene_input" id="${prefix}">
              <select class="tgene_source meme" name="${prefix}_source">
                <!--{category}-->
                <optgroup label="${name}">
                  <!--{listing}--><option value="${id}" XXXXselectedXXXX><!--{name}-->Listing Name<!--{/name}--></option>
                  <!--{/listing}-->
                </optgroup>
                <!--{/category}-->
              </select>
            </div>
          </div>
    <!--{/component}-->
  </body>
</html>
